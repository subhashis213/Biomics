# Copy to android/keystore.properties and fill in after running:
#   npm run playstore:keystore
#
# NEVER commit keystore.properties or the .keystore file to git.

storeFile=../credentials/biomicshub-upload.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=biomicshub-upload
keyPassword=YOUR_KEY_PASSWORD
